import 'package:flutter/material.dart';

class NotivigationsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Noteivgations"),
    );
  }
}